{% extends "base.html.twig" %}
{%  block title 'Edit Pin #' ~ pin.id %}
{% block body %}
<div class="row">
    <div class="col-md-6 mx-auto">
        <h1>Edit Pin # {{ pin.id }}</h1>

        {{ include('partials/_form.html.twig', {'submitButtonText' : 'Edit pin'}) }}
    </div>
</div>
{% endblock %}
